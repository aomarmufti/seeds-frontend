import ComingSoon from '@/components/ComingSoon';

export default function Page() {
  return (
    <ComingSoon
      title="Bookings"
      intro="Every lesson booked across the platform."
      legacy="adLoadAnalytics()"
    />
  );
}
